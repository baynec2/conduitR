% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_assay_by_annotation.R
\name{aggregate_assay_by_annotation}
\alias{aggregate_assay_by_annotation}
\title{Aggregate an Assay to Annotation Terms}
\usage{
aggregate_assay_by_annotation(
  qf,
  i,
  fcol,
  name = fcol,
  include_na = c("drop", "group"),
  na.rm = TRUE,
  fun = NULL
)
}
\arguments{
\item{qf}{A QFeatures object.}

\item{i}{Name of the base assay to aggregate from.}

\item{fcol}{Name of a rowData column on assay \code{i}. Either a list-column
(each entry a character vector of terms) or an atomic character/factor
column.}

\item{name}{Name of the new aggregated assay (defaults to \code{fcol}).}

\item{include_na}{One of \code{"drop"} (features with no annotation are
excluded from the aggregation; column totals shrink) or \code{"group"}
(unannotated features are collected into an \code{"Unassigned"} term so
intensity is conserved).}

\item{na.rm}{Passed to \code{fun} for handling NA intensity values during
summation. Independent of \code{include_na}.}

\item{fun}{Summation function passed to \code{\link[QFeatures:QFeatures-aggregate]{QFeatures::aggregateFeatures()}}.
If \code{NULL}, defaults to \link[MsCoreUtils:aggregate]{MsCoreUtils::colSumsMat} for list-columns and
\link[base:colSums]{base::colSums} for atomic columns.}
}
\value{
A QFeatures object with one new assay named \code{name}.
}
\description{
Aggregates features of a base assay into a new assay whose rows are the
levels of an annotation column. The annotation column may be either a
list-column (e.g. GO term sets, where one feature can map to many terms)
or an atomic column (e.g. species, where each feature maps to exactly one
term).
}
