% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregation_targets.R
\name{aggregation_targets}
\alias{aggregation_targets}
\title{Get registered aggregation targets}
\usage{
aggregation_targets(qf)
}
\arguments{
\item{qf}{A QFeatures object.}
}
\value{
A named list. Each element corresponds to a rowData column name
that can be aggregated against, and contains:
\itemize{
\item \code{kind}: one of \code{"functional"}, \code{"taxonomic"} (more values may
be added in the future).
\item \code{from}: the name of the base assay this target is aggregated
from.
}
Returns an empty list if nothing has been registered.
}
\description{
Returns the registry of annotation columns that have been added to a
QFeatures object and are eligible for on-demand aggregation via
\code{\link[=aggregate_assay_by_annotation]{aggregate_assay_by_annotation()}}. Stale entries (those whose base assay
or column no longer exists in the object) are silently filtered out.
}
\details{
Targets are registered by \code{\link[=add_annotation_to_qf]{add_annotation_to_qf()}} (with \code{kind = "functional"}) and \code{\link[=add_taxonomy_to_qf]{add_taxonomy_to_qf()}} (with \code{kind = "taxonomic"}).
}
